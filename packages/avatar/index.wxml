<view class="x-class x-avatar {{ shape }} x-avatar-{{ size }} {{ src ? 'x-avatar-image' : '' }}" style='width:{{size}}px;height:{{size}}px;line-height:{{size}}px;font-size:{{size/2+2}}px'>
    <image src="{{ src }}" wx:if="{{ src !== '' }}" class="width_100 height_100"></image>
    <view class="x-avatar-string" wx:else><slot></slot></view>
</view>