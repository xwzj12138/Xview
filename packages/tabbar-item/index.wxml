<view class='actionItem' style="{{width?'width:'+width+'rpx':''}}">
  <view class='x-class'>
    <view>
      <image wx:if='{{src}}' src='{{src}}'></image>
      <x-icon wx:elif='{{icon}}' name='{{icon}}' size='{{iconSize}}'></x-icon>
    </view>
    <view>{{title}}</view>
    <slot></slot>
  </view>
</view>